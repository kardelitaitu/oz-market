# CI Commands - Credit/Balance DB Schema and Domain Logic

Execute these commands to verify implementation of this spec:

```bash
# Run migrations and confirm compilation
cd backend && cargo check --all-targets

# Execute server tests
cargo test --package oz-market-server
```
