# CI Commands

Run from repository root:

```powershell
cargo check --manifest-path backend/server/Cargo.toml --workspace
```

Canonical benchmark command source of truth:

- `docs/server/README.md` (HTTP Bench Baseline section)
- `docs/testing/benchmarks/http-bench-baseline-2026-05-12.md`
