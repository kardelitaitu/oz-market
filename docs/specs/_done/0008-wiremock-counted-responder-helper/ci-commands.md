# CI Commands - Reusable Counted Responder

```bash
# Run unit and integration tests to verify the helper works
cargo test --manifest-path mobile/marketplace/src-tauri/Cargo.toml --lib -- client::sse::integration
```
