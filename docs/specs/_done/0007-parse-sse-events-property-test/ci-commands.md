# CI Commands - Property Test for parse_sse_events

```bash
# Run unit and property tests in the mobile client
cargo test --manifest-path mobile/marketplace/src-tauri/Cargo.toml --lib -- client::sse::tests
```
