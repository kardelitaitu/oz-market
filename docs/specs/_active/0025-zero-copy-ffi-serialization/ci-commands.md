# CI Commands - Zero-Copy FFI Serialization

Execute these commands to verify implementation of this spec:

```bash
# Compile mobile Rust binaries
cd mobile/marketplace/src-tauri && cargo check

# Run Svelte linter checks
cd .. && npm run svelte-check
```
