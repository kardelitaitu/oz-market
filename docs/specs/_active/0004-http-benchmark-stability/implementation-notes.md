# Implementation Notes

## Current Execution Notes

- Benchmark mode selector and failure counters are already implemented in `bench_concurrent`.
- Runtime and handler status-code parity checks were recently aligned.

## Remaining Work in This Spec

- keep artifact cadence and dated baseline updates consistent
- revalidate benchmark assumptions when contract-sensitive routes change
- maintain parity report snapshot on each benchmark cycle
