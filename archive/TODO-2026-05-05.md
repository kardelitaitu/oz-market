# TODO

A checklist of tasks. Use `~~strikethrough~~` only after the task is validated complete through tests or a documented verification step.

## Priority Decision

Do `executable spec validation` first.

## Why This Comes First

| Option | Pros | Cons | Recommendation |
| --- | --- | --- | --- |
| executable spec validation first | locks contract quality early, reduces backend drift, makes scaffold work safer | delays code scaffolding slightly | Best |
| backend scaffold work first | faster visible code progress | high risk of baking unstable contract assumptions into code | Weak |

Short reason:

- the repo already has a strong whitepaper and spec set
- the biggest remaining risk is contract drift, not missing folders
- validating the spec first gives a reliable base for Rust scaffolding

## Current Build Order

### Phase 1: Executable Validation And Alignment

- [x] ~~final review on `docs/whitepaper`~~
- [x] ~~finalize `docs/specs/openapi.yaml` as the implementation contract~~
- [x] ~~turn `docs/specs/validation-checklist.md` into actual validation commands later~~
- [x] ~~keep `docs/specs/spectral-rules.md` as the lint-policy source~~
- [x] ~~keep `docs/specs/redocly-notes.md` as the validator-policy source~~
- [x] ~~define the first CI command set for:~~
  - [x] ~~`yamllint`~~
  - [x] ~~`@redocly/cli`~~
  - [x] ~~`@stoplight/spectral-cli`~~
  - [x] ~~`oasdiff`~~
- [x] ~~milestone 1: validation pipeline is executable~~ (actual: `validation-checklist.md`, `ci-commands.md`, `redocly.yaml`, `.spectral.yaml`, `yamllint.yaml`, and the `oasdiff` wrapper all run against the frozen spec)
- [x] ~~milestone 2: contract alignment across client surfaces~~ (actual: HTTP runtime shell, MCP delegation, and mobile scaffolds all point at the frozen contract)
  - [x] HTTP (actual: shared `MarketplaceApp` now backs the HTTP runtime shell and uses the frozen contract)
  - [x] MCP (actual: `MarketplaceMcp` delegates to the shared app and reuses the same search/idempotency flow)
  - [x] Android (actual: scaffold folder + `contract-manifest.json` point at `docs/specs/openapi.yaml`)
  - [x] iOS (actual: scaffold folder + `contract-manifest.json` point at `docs/specs/openapi.yaml`)

### Phase 2: Backend Runtime And Facade Wiring

- [x] ~~scaffold Rust workspace under `backend/`~~ (actual: Cargo.toml exists)
- [x] ~~create shared crates:~~
  - [x] ~~`backend/crates/api-contract`~~ (actual: ~400 lines, types match OpenAPI)
  - [x] ~~`backend/crates/auth-core`~~ (actual: ✅ JWT validation + authorization implemented)
- [x] ~~scaffold transports:~~
  - [x] ~~`backend/server`~~ (actual: shared HTTP runtime and service wiring around the frozen contract)
  - [x] ~~`backend/mcp`~~ (actual: shared MCP facade delegating to the same app logic)
- [x] ~~wire the scaffold to the frozen OpenAPI contract, not ad hoc request shapes~~ (actual: ✅ server uses api-contract + auth-core)
- [x] ~~HTTP runtime shell~~ (actual: async TCP listener with health/search/get/create routes)

### Phase 3: Implementation After Validation

#### Implemented Behavior

- [x] ~~`listings` behavior~~ (actual: implemented in server service layer)
- [x] ~~`negotiations` behavior~~ (actual: implemented in server service layer)
- [x] ~~`audit_events`~~ (actual: Postgres-backed append-only audit log + transaction-ready append hook + server write-path emission)
- [x] ~~`outbox_events`~~ (actual: Postgres-backed append-only outbox queue + transaction-ready append hook + server write-path emission)
- [x] ~~authz enforcement behavior~~ (actual: implemented in auth-core with JWT validation + authorization)
- [x] ~~idempotency enforcement behavior~~ (actual: shared app + HTTP handlers use the idempotency guard and tests cover replay/conflict behavior)
- [x] ~~indexed search behavior~~ (actual: search service, deterministic repository filtering, and GIN `search_text` indexing are already in place)

#### Durable Persistence

- [x] ~~`reservation_leases` persistence~~ (actual: Postgres-backed lease repository wired into runtime with double-sell protection)
- [x] ~~`contact_reveals` persistence~~ (actual: Postgres-backed reveal repository wired into runtime with approve flow)

#### Contracts Defined

- [x] ~~repository contracts defined for listings, negotiations, reservations, contact reveals, audit events, outbox events, seller accounts, and agent credentials~~ (actual: listings + negotiations implemented, the rest are traits)

## Working Rule

- update this TODO when build order changes
- prefer contract-first implementation
- do not start transport code before validation policy is executable

## Status Note (2026-05-05)

This checklist now matches the implemented backend paths for the current build order:

- Phase 1: executable validation is in place
- Phase 2: backend scaffolding is implemented across the active crates and transports
- Phase 3: the listed backend implementation items are complete, including idempotency and indexed search
