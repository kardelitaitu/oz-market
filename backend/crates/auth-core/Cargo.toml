[package]
name = "marketplace-auth-core"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
jsonwebtoken = "9"
thiserror = "2"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
serde_json = "1"
