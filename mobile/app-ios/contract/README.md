# iOS Contract Scaffold

This folder keeps the iOS client aligned with the frozen API contract.

## Scope

- use `../../docs/specs/openapi.yaml` as the contract source
- keep request and response shapes canonical
- avoid iOS-only payload variants
