# Android Setup Scaffold

This folder keeps the Android app setup aligned with the mobile contract.

## Scope

- wire the user sign-in and backend token path
- keep `openrouter/free` setup app-scoped
- preserve the same retry and idempotency rules as the backend contract
